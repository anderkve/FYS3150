# Project title

This repository contains the code for [Project name] in the course FYS3150/FYS4150 at the University of Oslo. 
The project is developed by: 

- [Name 1]
- [Name 2]
- [Name 3]

In this project we [short description of the problem you solve and the method/methods used for solving it, and if relevant that you compared methods (1-3 sentences)]. 
The calculations and logic are implemented in C++, while Python is used for visualizing the results. 


## Repository structure

The repository is organised using the src/include convention, and the file tree is as follows: 
```text
├── include/
│   ├── algorithms.hpp
│   └── helper_functions.hpp
├── src/
│   ├── main.cpp
│   ├── algorithms.cpp
│   └── helper_functions.cpp
├── python/
│   └── plot_results.py
├── data/
│   └── ...   
├── figures/ 
│   └── ... 
└── README.md
```
(This pretty tree-thingy is not strictly necessary, but it is helpful for visualizing the structure - it also looks very cool. To obtain the tree-thingy for the directory you are currently in do one of the following. On windows simply run: 'tree' in the terminal and you should get something that looks like the tree above. For mac users install it using brew with the command 'brew install tree' and run the command 'tree'. For Linux users with ubuntu run the command: "sudo apt install tree", then run the command 'tree')

- `include/` contains header files
- `src/` contains source files 
- `python/` contains the script used for visualizing and plotting 
- `data/` contains the output data generated by the C++ scripts 
- `figures/` contains the figures generated by the python script


## Requirements 
The C++ code requires a compiler supporting C++20. 

The python scripts require Python 3 and the packages NumPy and Matplotlib.


## Building and running the code 
To build the code run the command

```
g++ -std=c++20 -O2 -Iinclude src/main.cpp src/algorithms.cpp src/helper_functions.cpp -o main
```

To run the program, do

```
./main 
```

The generated data is stored in the `data/` directory. 

## Plotting
Generate the plots with: 

`python3 python/plot_results.py`

The corresponding files are automatically stored in the `figures/` directory. 

